---
description: Standardize newly added files to the WDB protocol via the wdb-curator agent
argument-hint: "[path or files] (optional — defaults to your new/changed files)"
---
Use the wdb-curator subagent to standardize the files I just added (or those in
$ARGUMENTS, if provided) to the WDB add-to-brain protocol — correct initiative folder,
lower_snake_case name, and the required context note with cross-initiative
Related-files links — then report the pre-submit checklist result.
